Conexion a base de datos 
 user: "bwunnmjm",
  host: "baasu.db.elephantsql.com",
  database: "bwunnmjm",
  password: "yLZKnrpsGYcrMA0Dk-EEVzvNMIsLXK1c",
  port: "5432",

usar virtualenv y pip, el pip ya viene por defecto instalado con python

luego instalar virtualenv con pip : pip install virtualenv 
creas un entorno virtual con: python -m virtualenv env
luego:
    cd env
        cd Scripts  
            activate

luego las librerias estan documentadas en el archivo requirements.txt
luego pip install -r requirements.txt 

luego pyhton main.py

